# Decisions — <NAME>

Project analysis decision log. Dated, one line per non-obvious call (threshold, dropped sample,
filter, method choice), each linked to the commit that enacted it. Date header from the system
clock (never hand-typed). Written via `/decide`. Strategic/cross-project calls live in the hub's log.

## <YYYY-MM-DD>
- <decision — why> [<commit>]
